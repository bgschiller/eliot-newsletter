---
import { render, getCollection } from "astro:content";

const pages = await getCollection("pages");
const page = pages.find((p) => p.id === "speaker-message");

if (!page) return Astro.redirect("/404");

const { Content } = await render(page);

const title = page.data.title ?? "Speaker Message";
const attribution = page.data.attribution;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>Eliot Newsletter — {title}</title>
    <link rel="stylesheet" href="/styles/global.css" />
    <style is:inline>
      /* ============================================
         Speaker Message — Single-Column Layout
         8.5×11 print
         ============================================ */

      .speaker-message {
        max-width: 38rem;
        margin: 0 auto;
      }

      .speaker-title {
        font-family: var(--font-body);
        font-size: 1.25rem;
        font-weight: bold;
        color: var(--color-accent);
        margin-bottom: 1.25rem;
        text-align: center;
      }

      .speaker-message h2 {
        font-size: 1.125rem;
        color: var(--color-accent);
        margin-top: 1.5rem;
        margin-bottom: 0.4rem;
      }

      .speaker-message h3 {
        font-size: 1rem;
        font-weight: bold;
        color: var(--color-text);
        margin-top: 1.25rem;
        margin-bottom: 0.3rem;
      }

      .speaker-message p {
        margin-bottom: 0.7em;
        line-height: 1.5;
      }

      .speaker-message ul,
      .speaker-message ol {
        margin-bottom: 0.7em;
        padding-left: 1.5rem;
      }

      .speaker-message li {
        margin-bottom: 0.25em;
        line-height: 1.4;
      }

      .speaker-attribution {
        margin-top: 2rem;
        text-align: right;
        font-style: italic;
        color: var(--color-muted);
      }

      /* ============================================
         PRINT — 8.5×11 letter (single column)
         ============================================ */
      @media print {
        @page {
          size: 8.5in 11in;
          margin: 1in;
        }

        body {
          max-width: none;
          padding: 0;
          background: white;
          color: black;
        }
        body::before {
          display: none;
        }

        .back-link {
          display: none;
        }

        .speaker-message {
          max-width: none;
        }

        .speaker-title {
          font-size: 16pt;
          font-weight: bold;
          color: black;
          text-align: left;
          margin-bottom: 14pt;
        }

        .speaker-message h2 {
          font-size: 14pt;
          font-weight: bold;
          color: black;
          margin-top: 12pt;
          margin-bottom: 4pt;
        }

        .speaker-message h3 {
          font-size: 12pt;
          font-weight: bold;
          color: black;
          margin-top: 10pt;
          margin-bottom: 3pt;
        }

        .speaker-message p {
          font-size: 11pt;
          margin-bottom: 0;
          orphans: 3;
          widows: 3;
        }

        .speaker-message ul,
        .speaker-message ol {
          font-size: 11pt;
        }

        .speaker-message li {
          font-size: 11pt;
        }

        .speaker-attribution {
          font-size: 10pt;
          font-style: italic;
          color: black;
          margin-top: 18pt;
        }

        a[href]::after {
          content: none !important;
        }
      }
    </style>
  </head>
  <body>
    <nav class="back-link">
      <a href="/">&larr; Back to Newsletter</a>
    </nav>
    <main class="speaker-message">
      <h1 class="speaker-title">{title}</h1>
      <Content />
      {attribution && (
        <p class="speaker-attribution">&mdash;{attribution}</p>
      )}
    </main>

  </body>
</html>
